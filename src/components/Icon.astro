---
const {
  icon,
  alt,
  width,
  height,
  class: className,
} = Astro.props;
---

<img
  src={icon}
  alt={alt}
  width={width}
  height={height}
  class={className}
/>

<style lang="scss">
  @use '../styles/mediaqueries' as mq;
  @use '../styles/functions' as fn;
</style>
