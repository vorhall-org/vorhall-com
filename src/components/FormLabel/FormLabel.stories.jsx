import Comp from './FormLabel.jsx';

export default {
  component: Comp,
};

const defaultArgs = {
  classes: '',
  inputId: '',
  text: 'Form label text',
};

export const FormLabel = {
  args: defaultArgs,
};
