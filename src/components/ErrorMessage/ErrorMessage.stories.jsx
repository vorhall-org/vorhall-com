import Comp from './ErrorMessage.jsx';

export default {
  component: Comp,
};

const defaultArgs = {
  classes: '',
  text: 'some error message text',
};

export const ErrorMessage = {
  args: defaultArgs,
};
