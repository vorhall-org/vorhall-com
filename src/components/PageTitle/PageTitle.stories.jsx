import Comp from './PageTitle.jsx';

export default {
  component: Comp,
};

const defaultArgs = {
  classes: '',
  highlight: 'with highlight',
  text: 'Page title text',
};

export const PageTitle = {
  args: defaultArgs,
};
