---
const {
  title,
  body,
} = Astro.props;
---

<li class='container'>
  <div class='container-wrapper'>
    <h2>
      {title}
      <span>&rarr;</span>
    </h2>
    <p>
      {body}
    </p>
  </div>
</li>

<style lang='scss'>
  @use '../styles/mediaqueries' as mq;
  @use '../styles/functions' as fn;

  .container {
    list-style: none;
    display: flex;
    padding: fn.to-rem(1);
    background-color: #23262d;
    background-image: none;
    background-size: 400%;
    border-radius: fn.to-rem(7);
    background-position: 100%;
    transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 10%);
  }

  .container > .container-wrapper {
    width: 100%;
    text-decoration: none;
    line-height: 1.4;
    padding: fn.to-rem(23);
    border-radius: fn.to-rem(8);
    color: rgb(var(--foreground-rgb));
    background-color: #23262d;
    opacity: 0.8;

    @include mq.breakpoint($from: medium) {
      color: red;
    }
  }

  h2 {
    margin: 0;
    font-size: fn.to-rem(20);
    transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }

  p {
    margin-top: fn.to-rem(8);
    margin-bottom: 0;
  }

  .container:is(:hover, :focus-within) {
    background-position: 0;
    background-image: var(--accent-gradient);
  }

</style>
