---
import i18n from '../i18n/i18n-impressum';
import { useTranslations } from '../i18n/utils';
import { getLocale } from 'astro-i18n-aut';
import pageDataGetterGeneral from '../content-helpers/general';

import Layout from '../layouts/Layout.astro';
import Main from '../components/MainContainer/MainContainer';
import Section from '../components/Section/Section';
import Jumbotron from '../components/Jumbotron/Jumbotron';
import Richtext from '../components/Richtext/Richtext';

const locale = getLocale(Astro.url);
const t = useTranslations(locale, i18n);
const pageDataGeneral = pageDataGetterGeneral(locale);

const pageMetaInformation = {
  tags: [
    {
      content: t('description'),
      name: 'description',
    },
    {
      content: 'noindex',
      name: 'robots',
    },
  ],
  title: t('docTitle'),
};

const jumbotronData = {
  heading: {
    level: 1,
    text: t('title'),
    visualLevel: false,
  },
  leadTextContent: t('lead'),
  mainJumbo: false,
};

---

<Layout
  pageMetaTags={pageMetaInformation.tags}
  pageTitle={pageMetaInformation.title}
>
  <Main {...pageDataGeneral.mainData}>

    <Section>
      <Jumbotron {...jumbotronData} />
      <Richtext text={t('richtext')} />
    </Section>

  </Main>
</Layout>
