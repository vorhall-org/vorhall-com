import Comp from './SuccessMessage.jsx';

export default {
  component: Comp,
};

const defaultArgs = {
  classes: '',
  text: 'Some success message text',
};

export const SuccessMessage = {
  args: defaultArgs,
};
